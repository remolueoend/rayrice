# see: https://github.com/abraunegg/onedrive/blob/master/docs/USAGE.md#configuration

# if sync_list in use, see `./sync_list`

# Directory where the files will be synced
sync_dir = "~/OneDrive"
# Skip files and directories that match this pattern
skip_file = "~*|.~*|*.tmp"

# skip folders containing an empty .nosync file
check_nosync = "true"

# save battery
monitor_interval = "300"
